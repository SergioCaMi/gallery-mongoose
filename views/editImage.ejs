<%- include("template/cabecera", { title: title }) %>

<div class="gallery container" style="display: flex; justify-content: center;">
  <div class="card" style="min-width: 800px; height: 800px; display: flex; justify-content: center; align-items: center;">
    <img src="<%= image.urlImagen %>" alt="<%= image.title %>" style="width: 100%; height: 100%; object-fit: cover;">
  </div>
</div>

<form class="formulario container" id="imageForm" action="/edit-image" method="POST">
  <!-- Enviamos el ID de la imagen -->
  <input type="hidden" name="id" value="<%= image._id %>">

  <div class="form-group">
    <label for="title">Título:</label>
    <input type="text" id="title" name="title" maxlength="30" pattern="^[a-zA-Z0-9 _]+$" value="<%= image.title %>" required>
  </div>

  <div class="form-group">
    <label for="date">Fecha:</label>
    <input type="date" id="date" name="date" value="<%= image.date %>" required>
  </div>

  <div class="form-group">
    <label for="description">Descripción:</label>
    <textarea id="description" name="description" maxlength="4000" rows="6"><%= image.description %></textarea>
  </div>

  <button type="submit" class="submit-btn">📤 Guardar cambios</button>
</form>

<%- include("template/footer") %>

<script>
  // Si necesitas acceder al objeto image desde JS:
  const image = <%= JSON.stringify(image) %>;
</script>
</body>
</html>